Allowed labels: {allowed_labels}
Label definitions:
{defs_block}

Retrieved labeled examples:
{few_shots}

CLASSIFY THIS QUERY:
{query}

Return exactly this JSON schema:
{{ "label": "<allowed label>", "confidence": <number 0..1> }}
Rules:
- Choose exactly ONE label from Allowed labels.
- Match the EXACT case and format of labels in Allowed labels.
- If unsure, pick the best label and set a low confidence.
- confidence must be between 0 and 1.
