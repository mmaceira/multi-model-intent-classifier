# Experiment config: CLINC150 full dataset (default).
#
# Thin override layered on top of:
#   - config/base/defaults.yaml
#   - config/base/providers.yaml
#   - config/datasets/clinc150.yaml

general:
  run_name: "experiment_default"
