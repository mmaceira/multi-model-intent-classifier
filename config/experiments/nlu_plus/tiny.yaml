# Experiment config: NLU+ tiny multi‑label subset.
#
# Mirrors the legacy `config/dataset/nlu_plus/tiny.yaml` while relying on:
#   - config/base/defaults.yaml
#   - config/base/providers.yaml
#   - config/datasets/nlu_plus.yaml

general:
  run_name: "experiment_multilabel_nlu_tiny"

dataset:
  max_train_samples: 100
  max_val_samples: 50
  max_test_samples: 50

model:
  embedding_backend: "sbert"
  sbert_model_name: "sentence-transformers/all-MiniLM-L6-v2"
  openai_model_name: "text-embedding-3-small"
  ollama_embedding_model_name: "ollama/qwen3-embedding:latest"

  classifier: "linear_svm"
  rag_top_k: 5
